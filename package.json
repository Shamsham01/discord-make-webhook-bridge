{
  "name": "discord-make-webhook-bridge",
  "version": "2.0.0",
  "description": "A multi-server Discord bot that runs named Make.com workflows through mentions, replies, slash commands and optional AI routing.",
  "type": "module",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js",
    "test": "node --test",
    "check": "node --check src/index.js && node --check src/commands.js && node --check src/store.js && node --check src/webhook.js && node --test"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "dependencies": {
    "discord.js": "^14.27.0",
    "dotenv": "^17.4.2"
  },
  "license": "MIT",
  "keywords": [
    "discord",
    "make.com",
    "webhook",
    "chatbot",
    "automation",
    "ai-router"
  ]
}
